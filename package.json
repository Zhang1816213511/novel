{
  "name": "novel-writer",
  "version": "1.0.0",
  "description": "本地写作智能助手 - 桌面客户端",
  "private": true,
  "main": "electron/main.js",
  "scripts": {
    "electron:dev": "electron .",
    "electron:build": "electron-builder --win --config electron/builder.json",
    "electron:build:dir": "electron-builder --win --config electron/builder.json --dir"
  },
  "devDependencies": {
    "electron": "^29.0.0",
    "electron-builder": "^24.13.0"
  }
}
